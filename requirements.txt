# =====================================================================
# Legal AI Agent — Python Dependencies
# =====================================================================
# Install: pip install -r requirements.txt
# Python version: 3.11+ recommended (3.10 minimum)

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Database (PostgreSQL / Supabase) ---
psycopg2-binary>=2.9.9
supabase>=2.4.0

# --- HTTP Clients ---
httpx>=0.27.0
requests>=2.31.0

# --- Data Validation ---
pydantic[email]>=2.7.0

# --- Authentication & Security ---
bcrypt>=4.1.0
PyJWT>=2.8.0
cryptography>=42.0.0

# --- File Handling ---
python-multipart>=0.0.9
aiofiles>=23.2.1

# --- Environment ---
python-dotenv>=1.0.0

# --- Text Processing ---
unidecode>=1.3.8
beautifulsoup4>=4.12.3

# --- Document Parsing & Generation ---
# PDF parsing (python-docx covers DOCX read/write; pypdf handles PDF extraction)
pypdf>=4.2.0
python-docx>=1.1.0

# --- LLM SDKs ---
# openai is used by llm_provider.py for the Ollama OpenAI-compatible endpoint.
# The anthropic SDK is listed for potential future use / optional multi-LLM support.
openai>=1.30.0
anthropic>=0.28.0

# =====================================================================
# Dev / Test dependencies (not needed for production runtime)
# Install with: pip install -r requirements-dev.txt
# =====================================================================
